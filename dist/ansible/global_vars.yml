
org: "ignalina"
os_version: "fedora_41" 

org_group: "{{ org }}"
org_user: "{{ org }}"

install_home: "/opt/{{ org }}/{{ os_version }}"
airgap_home: "{{ install_home }}/airgap"
data_home: "/var/data/{{ org }}"
logs_home: "/var/logs/{{ org }}"
pids_home: "{{ data_home }}/pids"


{{ spark_home }}/spark-{{ spark_version }}-bin-hadoop3.2

spark_version: "3.5.3"
spark_airgapl: "{{ airgap_home }}/spark/{{ spark_version }}"
spark_home: "{{ install_home }}/spark/{{ spark_version }}"
spark_user: "spark"
spark_group: "spark"
spark_master_url: "spark://{{ hostvars[master-node].ansible_host }}:7077"
